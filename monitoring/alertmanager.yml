global:
  resolve_timeout: 5m

route:
  receiver: "default"
  group_by: ["alertname", "job"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: "default"
    # Example email configuration (placeholder).
    # Uncomment and adjust for your SMTP relay:
    # email_configs:
    #   - to: "alerts@example.com"
    #     from: "lumen-alerts@example.com"
    #     smarthost: "smtp.example.com:587"
    #     auth_username: "user"
    #     auth_password: "pass"

