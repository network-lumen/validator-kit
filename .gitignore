# Local environment and secrets

# Per-service env files
.env
ops/headscale/.env
ops/monitoring/.env

# Headscale auth keys and TLS certs
ops/headscale/headscale_keys_*.txt
ops/headscale/proxy/certs/

# Headscale state backups
ops/headscale/backups/

# Local Lumen binary used by helper scripts
bin/lumend
