# Local environment and secrets (deploy repo)

# Per-service env files
.env
headscale/.env
monitoring/.env

# Headscale auth keys and TLS certs
headscale/headscale_keys_*.txt
headscale/proxy/certs/

# Headscale state backups
headscale/backups/

# Local Lumen binary used by helper scripts
bin/lumend
